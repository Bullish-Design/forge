# ASSUMPTIONS

- User wants Docker-specific operational files consolidated into `docker/`.
- Running commands from repo root should remain supported.
- `.env` should live at repo root for standard docker compose variable loading.
- A practical exception is acceptable when required by Docker tooling behavior.
