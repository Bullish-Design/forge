# ASSUMPTIONS

- Demo consumers have local access to all three repos:
  - `forge`
  - `obsidian-agent`
  - `obsidian-ops`
- `devenv` is installed and usable in each repo.
- `jj` is installed (required for undo/commit behavior).
- A valid LLM backend is available (Anthropic or OpenAI-compatible).
