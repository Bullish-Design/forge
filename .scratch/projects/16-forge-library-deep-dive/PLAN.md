# PLAN

Objective: produce a deep, implementation-grounded explanation of Forge covering identity, capability, architecture, and setup.

Execution plan used:

1. Establish product positioning and exact scope from top-level docs.
2. Trace public CLI surface from `cmd/forge` and `internal/cli`.
3. Trace build/render pipeline from `internal/builder` and `internal/obsidian`.
4. Trace dev/runtime request path from `internal/server`, `internal/proxy`, `internal/overlay`, and `internal/watch`.
5. Extract setup paths from `init/generate/serve/dev`, demo scripts, and Docker stack.
6. Write four detailed analysis/review documents and cross-link them.

